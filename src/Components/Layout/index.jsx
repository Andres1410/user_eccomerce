const Layout = ({ children }) => {
  return (
    // Aqui se renderiza layouts 🤣🤣🤣
    <div className='flex flex-col items-center mt-20'>
      {children}
    </div>
  )
}

export default Layout